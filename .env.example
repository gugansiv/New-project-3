# ─── Database ────────────────────────────────────────────────────
DB_USER=ignite
DB_PASSWORD=CHANGE_ME_DB_PASS
DB_NAME=ignite_qsr

# ─── JWT (generate with: openssl rand -hex 32) ───────────────────
JWT_SECRET=CHANGE_ME_JWT_SECRET

# ─── Razorpay ────────────────────────────────────────────────────
NEXT_PUBLIC_RAZORPAY_KEY=rzp_live_CHANGE_ME
RAZORPAY_SECRET=CHANGE_ME_RAZORPAY_SECRET

# ─── Admin credentials seeded on first boot ──────────────────────
# (handled by the app's initDbTables on first run)
